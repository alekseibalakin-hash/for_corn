# Бриф — Фаза A: хаб + переиспользуемый наградный слой

> Вставь блок ниже в кодинг-сессию. Источник правды — DESIGN-HUB.md. Это РЕФАКТОР живого
> приложения: главное — не потерять данные жены (кошелёк, completed, партию). Match-3 пока
> НЕ делаем, только плитка «скоро».

---

```
Фаза A расширения «2048 с любовью» до ХАБА игр (та же кодовая база). Цель: вынести наградный
слой в переиспользуемый модуль и добавить экран-хаб, НЕ ломая живую игру и НЕ потеряв данные
жены (кошелёк, completed, текущую партию). Сверяйся с DESIGN-HUB.md — источник правды.

Работай ТОЛЬКО в этой папке. Прочитай: DESIGN-HUB.md и текущие src/ui/useGame.ts,
src/ui/App.tsx, src/engine/* , src/storage/* , content/achievements.json.

1) Наградный слой (игро-независимый). Вынеси из useGame.ts общую часть в src/rewards/
   (хук useRewards или контекст RewardsProvider): wallet, history, progress, rewardsRedeemed,
   dailyStreak, evaluate+выдача купонов, redeem→completed, очередь раскрытий (RevealModal),
   victory, expiry-баннер, персист ОБЩИХ ключей (wallet/history/progress). Экспонируй:
   grant(gameId, gameSnapshot), redeem, collectReveal, dismiss*, и состояние
   wallet/activeReveal/redeemCelebration/reminder/showVictory + completedCount/totalAchievements
   (по ВСЕМУ хабу). grant сам мержит globalStats (rewardsRedeemed, dailyStreak) в снапшот.
   useGame (2048) оставляет только 2048-состояние (доска, партия, 2048-статы, won) и зовёт
   rewards.grant('2048', snapshot2048) на ходах и смене партии.

2) Движок (src/engine): добавь в тип Achievement необязательное поле game. evaluateAchievements
   принимает gameId и фильтрует ачивки: (a.game ?? '2048') === gameId ИЛИ a.game === 'any'.
   Снапшот для оценки = merge(globalStats, gameSnapshot). Существующие 25 ачивок НЕ меняем
   (untagged трактуются как '2048'). Добавь юнит-тесты: фильтр по game, merge globalStats.

3) Неймспейс: 2048-статы остаются БЕЗ префикса (ачивки не трогаем). Заложи в коде соглашение
   m3_* для будущего match3 (сами ачивки m3 не добавляй).

4) Хранилище + МИГРАЦИЯ (бережно!):
   - НЕ менять и НЕ терять ключи wallet/history/progress — это данные жены.
   - rewardsRedeemed перенести из stats в общий слой (в progress): при загрузке, если
     progress.rewardsRedeemed не задан — засеять из stats.rewardsRedeemed. Лосслесс.
   - 2048 board/stats остаются на тех же ключах. Заведи неймспейс-ключи для match3 (match3.*),
     но без использования (готовность к фазе B).
   - НЕ поднимать STORAGE_VERSION (это сотрёт всё). Миграция строго аддитивная, без сброса.
     Механику ?reset=1 и schemaVersion не ломать.
   - dailyStreak — теперь хаб-стрик (играла в любую игру сегодня), владелец rewards.

5) Хаб-экран (src/ui/Hub.tsx) — новый корневой вид: плитки игр (2048 играбельна;
   «Match-3 — скоро ✨» задизейблена), вход в кошелёк, «выполнено X из N заданий» по хабу,
   тёплая палитра/Nunito. Роутинг простым состоянием view:'hub'|'g2048' (без react-router),
   из 2048 есть «назад» в хаб. Онбординг (первый запуск) — один раз на входе в хаб.
   Игры подключать через React.lazy + Suspense (ленивая загрузка), чтобы бандл не рос.

6) Метка сборки: подними BUILD_TAG в src/ui/constants.ts 'v4'→'v5' (видимая метка, чтобы
   глазами понять, что загрузилась новая сборка). STORAGE_VERSION НЕ трогай — иначе сотрёт
   данные жены.

НЕ трогать: content/rewards.json, деплой, бота, палитру/тон/анти-грайнд.

Definition of done:
- npm run typecheck, npm test (+ новые тесты: game-фильтр, merge-снапшот, миграция
  rewardsRedeemed), npm run build — всё зелёное.
- МИГРАЦИЯ доказана тестом: засей старое состояние (ключи wallet/history/progress/stats,
  schemaVersion='3', stats.rewardsRedeemed>0, купон в кошельке, id в progress.completed) →
  после загрузки: кошелёк, история, completed НА МЕСТЕ; «подарено N» = старому rewardsRedeemed;
  партия 2048 резюмится (не с нуля).
- 2048 играется из хаба идентично (свайпы/стрелки/награды/раскрытие/победа); плитка match3
  «скоро» задизейблена.
- 2048 грузится отдельным чанком (ленивая загрузка работает).
- Прогон вживую (браузер+mock): хаб → 2048 → ход → награда «Забрать» → «назад» в хаб → кошелёк.
- Отчёт: что вынесено в rewards/, изменения в движке/хранилище, как сделана миграция, что
  осталось для фазы B (match3).
```

---

## Что проверит CTO-сессия после фазы A
- Миграция: НИЧЕГО из wallet/history/progress/completed/«подарено N» не потеряно; партия 2048 резюмится.
- Наградный слой реально игро-независим (grant(gameId, snapshot), global-статы мержатся).
- Движок: фильтр по game корректен, существующие 25 ачивок работают как раньше (untagged→'2048').
- STORAGE_VERSION не поднят; ?reset=1 цел.
- Хаб/роутинг/ленивая загрузка; 2048 играбелен; тесты/сборка зелёные; тон/палитра целы.
