# Бриф — Match-3 v2: новые фишки

> Вставь блок в кодинг-сессию. Match-3 уже в проде (хаб, спецфишки line/colorBomb/bomb,
> тап-детонация, плавная анимация на id-адаптере gems.ts). Это следующий слой «вкуса».
> Игра для ОТДЫХА — никаких лимитов ходов/проигрыша/таймеров-давления.

---

```
Match-3 v2 (та же кодовая база, Match-3 уже в проде). Добавить «вкус» поверх готовой игры.
Работай ТОЛЬКО в этой папке. Прочитай: src/games/match3/{logic.ts,gems.ts,useMatch3.ts,Match3.tsx,
logic.test.ts,gems.test.ts}, src/engine/achievements.ts (PER_GAME_STATS), content/achievements.json.

ЖЁСТКИЕ РАМКИ (не сломать живой подарок):
- logic.ts: НЕ добавлять id в Gem/Board (id только в gems.ts) — иначе падают logic-тесты.
- Инвариант gems.test.ts (gemsToBoard(applyStep(prev,step)) === step.board) ДОЛЖЕН остаться
  зелёным — добавь кейсы на новые комбо. Это замок от десинка анимации с логикой.
- STORAGE_VERSION='3' не трогать; форму PersistedMatch3 (board без id) не менять; данные/2048/
  кошелёк/наградный слой/edge-triggering не трогать.
- Игра остаётся РАССЛАБЛЕННОЙ: без проигрыша, без лимита ходов. BUILD_TAG → v8 при выкатке.

1) ПОЛНЫЙ НАБОР СПЕЦ-КОМБО (своп двух соседних спецфишек). Сейчас есть базовые (проверь
   combineSpecials/resolveSwap в logic.ts: line+line→крест, bomb+bomb→5×5, colorBomb+colorBomb→
   всё поле). ДОБАВИТЬ недостающие:
   - line+bomb → «толстый крест»: очистить 3 ряда (центр±1) и 3 столбца (центр±1);
   - colorBomb+line → все фишки типа второй фишки становятся line-спецами и детонируют (цепная очистка);
   - colorBomb+bomb → все фишки этого типа становятся bomb-спецами и детонируют;
   - (line+line, bomb+bomb, colorBomb+обычная, colorBomb+colorBomb — оставить как есть, если уже есть).
   Реализация — в чистой логике (logic.ts), детонация идёт через существующий каскадный поток
   (resolveSwap → steps), визуал и FX уже работают на каскадах. Покрыть КАЖДОЕ новое комбо тестом
   (какие клетки очищаются) + добавить комбо-кейсы в gems.test инвариант.

2) ПОДСКАЗКА ПРИ ПРОСТОЕ (релакс-фича). Если ~5 секунд нет действий игрока И на поле есть валидный
   ход — мягко подсветить ОДНУ валидную пару фишек (лёгкий glow/пульс на двух клетках). Любое
   действие (свайп/тап/своп) сбрасывает подсказку и таймер. Никакого видимого таймера/счётчика —
   только ненавязчивая подсветка. Поиск валидной пары — переиспользуй перебор из hasAnyMove (logic.ts).
   Таймер чистить при unmount/смене партии/busy.

3) ПРАЗДНИК НА БОЛЬШОМ КЛИРЕ. Если за ОДИН ход убрано ≥20 фишек (см. biggestClear хода) — запустить
   конфетти (canvas-confetti, уже в проекте — см. src/ui/confetti.ts) + лёгкую вспышку/«бумц» поля
   (scale/opacity, transform-only, без звука). Только на крупных клирах, не на каждом ходу.

4) НОВЫЕ m3-СТАТЫ + АЧИВКИ.
   - Новый КУМУЛЯТИВНЫЙ стат m3_combos (сколько раз сделано спец-комбо за всё время) — инкремент
     в useMatch3, когда ход был комбо двух спецов; добавь в M3CumulativeStats/buildM3Snapshot/персист.
     НЕ добавлять в PER_GAME_STATS (кумулятивный → level).
   - m3_biggestClear уже есть (per-game, в PER_GAME_STATS).
   Добавь в content/achievements.json (game:'m3'):
     {"id":"m3-combo-master","type":"milestone","game":"m3","title":"Комбо-мастер 🎆","description":"Сделай 10 спец-комбо","trigger":{"stat":"m3_combos","op":">=","value":10},"rewardTier":"medium"},
     {"id":"m3-combo-legend","type":"milestone","game":"m3","title":"Повелитель комбо","description":"Сделай 50 спец-комбо","trigger":{"stat":"m3_combos","op":">=","value":50},"rewardTier":"large"},
     {"id":"m3-megaclear","type":"challenge","game":"m3","title":"Мегаклир","description":"Убери 30+ фишек одним ходом","trigger":{"stat":"m3_biggestClear","op":">=","value":30},"rewardTier":"large","cooldownDays":2}
   Награды — из общего каталога (rewardTier). content/rewards.json НЕ трогать.

Definition of done:
- npm run typecheck, npm test, npm run build — зелёное; Match3 — отдельный ленивый чанк.
- Тесты: каждое новое комбо (очищаемые клетки), gems-инвариант на комбо-шагах остаётся зелёным,
  m3_combos считается; существующие тесты целы.
- Прогон вживую (браузер, ?reset): спец-комбо срабатывают зрелищно (line+bomb, colorBomb+line/bomb);
  после ~5с простоя — подсказка валидной пары; клир ≥20 → конфетти+вспышка; своп/тап-детонация/2048
  целы; консоль чистая.
- Отчёт: что добавлено в logic/useMatch3/Match3, новые статы/ачивки, что осталось.
```

---

## Что проверит CTO-сессия после v2
- Каждое новое комбо покрыто тестом; gems-инвариант (gemsToBoard(applyStep)==step.board) зелёный на комбо-шагах.
- logic.ts без id; 2048/данные/кошелёк/edge-triggering/STORAGE_VERSION не тронуты; персист без id.
- Подсказка ненавязчивая, сбрасывается действием, чистит таймеры; игра осталась расслабленной (без лимитов).
- m3_combos кумулятивный (level), новые ачивки считаются; «выполнено X из N» вырос корректно.
- Живой прогон: комбо/подсказка/праздник + регресс-чек тапа/свопа/2048.
